{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Yukinal main window. The UI may open default browser URLs but may not open arbitrary files, shell out, or spawn the sidecar itself (-R9).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
