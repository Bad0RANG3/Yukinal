{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Yukinal main window. Core only: the UI may not shell out, read arbitrary files, or spawn the sidecar itself (-R9).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
