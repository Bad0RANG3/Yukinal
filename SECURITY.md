# 安全策略

Yukinal 可以持有 SSH 凭据、执行远程命令、读写远端文件并启动本地 MCP 子进程。安全缺陷按最高优先级处理；请不要在公开 Issue 中披露可利用细节。

## 支持的版本

| 版本 | 安全更新 |
| --- | --- |
| `1.0.x` | 支持 |
| `< 1.0.0` | 不再支持 |

## 私下报告

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。若该入口不可用，请通过维护者的 GitHub 个人资料联系，并先只发送最小信息，说明需要私密沟通；不要在公开渠道发送 PoC、凭据或完整日志。

报告建议包含：

- 受影响版本、操作系统和安装方式；
- 可复现步骤与最小 PoC；
- 实际影响，例如权限绕过、凭据泄露、命令注入、路径逃逸或 IPC 校验绕过；
- 已脱敏的日志与错误信息；
- 是否已经公开、是否依赖特定配置或竞态。

## 响应目标

维护者会尽快确认收到报告，并在完成初步分诊后给出影响判断与后续沟通时间。以下目标是协作预期，不是对修复时限的保证：

- 3 个工作日内确认收到；
- 7 个工作日内完成初步分诊；
- 根据严重度、可利用性和发布窗口安排修复与披露。

项目不提供漏洞赏金承诺。修复发布后，在征得报告者同意的前提下致谢。

## 范围重点

优先关注：

- 凭据从操作系统凭据库、sidecar 参数、日志、事件或 SQLite 中泄露；
- Permission Engine 被绕过、票据跨工具/目标/环境重放，或危险操作被自动批准；
- 服务器 id、工具名、路径、命令或主机指纹在校验后发生歧义；
- Tauri IPC、sidecar JSON-RPC 或 MCP 帧的 schema、长度或生命周期绕过；
- Agent 或 MCP 输出被当作可信指令执行；
- 退出、取消、超时或重启路径留下进程、句柄或未审计的执行。

安全模型、已有边界和明确不保证的内容见 [安全与数据边界](./docs/security.md) 与 [当前限制](./docs/limitations.md)。安装包当前未签名，这不属于未公开漏洞，但属于用户必须知道的发布限制。